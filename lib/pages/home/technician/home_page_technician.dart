import 'package:flutter/material.dart';

class HomePageTechnician extends StatelessWidget {
  const HomePageTechnician({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Technician'),
      ),
    );
  }
}
